<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      OFF.IA remains local-first. Network permission is used only by explicit
      online capabilities such as first-run model download and Curiosity.
      Transformer access is routed only through the future M2A2 boundary;
      llama.cpp, Memoria.ia and BDR remain local.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:icon="@drawable/offia_app_icon"
        android:roundIcon="@drawable/offia_app_icon"
        android:label="OFF.IA"
        android:supportsRtl="true"
        android:theme="@style/Theme.Offia">
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".LauncherActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
